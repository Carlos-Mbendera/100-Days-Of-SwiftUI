import Cocoa

// We're converting Celcius Degrees to Farenheight

var celDegree = 22.0
var farDegrees = ( (celDegree * 9) / 5 ) + 32
print("\(celDegree) Degrees in Celsius in \(farDegrees) Fahrenheit Degrees")
